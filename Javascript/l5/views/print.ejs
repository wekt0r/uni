<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<style>
    table {
        border-collapse: collapse;
        width: 1000px
    }
    input.number {
        width: 50px;
    }
    tr,th,td{
        border: 2px solid #444444;
        text-align: left;
        padding: 8px;
    }
</style>


<body>
    <form method="POST" >
        <div>
              <table border="1">
              <tr>
                <th>Przedmiot:</th>
                <td>
                    <%= course %>
                </td>
                <th>Imię i nazwisko:</th>
                <td>
                    <%= name %>
                </td>
                <th>Data: </th>
                <td>
                    <%= date?date:(new Date()).toLocaleDateString("pl-PL") %>
                </td>
                <th>Suma: </th>
                <td>
                    <%= sum?sum:z.reduce((x,y) => (x + (+y)), 0) %>
                </td>

              </tr>
          </table>
          <table>
              <tr>
                <th>
                Zadanie:
                </th>
                <% for (var i = 1; i <= 10; i++) { %>
                    <td>
                        <%= i %>
                    </td>
                <% } %>
              </tr>
              <tr>
                  <th>
                  Punkty:
                  </th>
                  <% for (var i = 0; i < 10; i++) { %>
                      <td>
                          <%= z[i] %>
                      </td>
                  <% } %>
              </tr>
            </table>
        </div>
    </form>
</body>

</html>
